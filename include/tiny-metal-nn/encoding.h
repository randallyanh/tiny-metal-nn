#pragma once

/**
 * @file encoding.h
 * @brief Encoding — Module subtype for input encodings (hash grid, etc.).
 */

#include "tiny-metal-nn/detail/module.h"

namespace tmnn {

class Encoding : public Module {};

} // namespace tmnn
